name: Ping Render
on:
  schedule:
    - cron: '*/5 * * * *'  
  workflow_dispatch:  

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          echo "Pinging Render app..."
          curl -f https://zorp-4mw6.onrender.com/ping || echo "Ping failed"
          echo "Ping completed at $(date)"
